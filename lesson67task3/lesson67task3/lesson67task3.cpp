#include <iostream>
#include <cmath>
#include <string>

/*int main()
{
    int outer = 1;
    int x = 5;
    while (x >= 1)
    {
        int inner = x;
        while (inner >= outer)

        {
            std::cout << inner-- << " ";
        }

        --x;
        std::cout << "\n";
    }

    return 0;
}*/

int main()
{
    int outer = 1;
    int x = 5;
    char ch('i');
    std::cout <<ch<<ch<<ch<<ch<<ch<<outer<<std::endl;
   /* while (x >= 1)
    {
        int inner = x;
        while (inner >= outer)

        {
            std::cout << inner-- << " ";
        }

        --x;
        std::cout << "\n";
    }*/

    return 0;
}


